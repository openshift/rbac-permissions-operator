# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
build/_output

### Vim ###
*.orig
# swap
.sw[a-p]
.*.sw[a-p]

### Boilerplate ###
.operator-sdk/*
.venv/*
# Artifacts from running locally app-sre pipeline
.docker/
Dockerfile.olm-registry
saas-rbac-permissions-operator-bundle

### Go ###
vendor/*
